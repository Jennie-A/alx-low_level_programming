#include "main.h"

/**
 * print_alphabet - print alphabet in lowercases followed by new a line
 *
 * Return: Always 0
 */
int main(void)
{
	char L;

	for (L = 'a'; L <= 'z'; L++)
		_putchar(L);
	_putchar('\n');
	return (0);
}
