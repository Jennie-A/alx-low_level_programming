#include "main.h"

/**
 * mul - The program writes a function that multiplies 2 integers
 * @a: first integer
 * @b: second integer
 * Return: results of the two multiplied integers
 */
int mul(int a, int b)
{
	return (a * b);
}
