#include "main.h"

/**
 * _abs - a function that computes the absolute integer
 *@i: the int to check
 * Return: the absolute value of int
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
