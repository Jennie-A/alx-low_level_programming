#include <stdio.h>
#include "main.h"

/**
 * main - the function prints the number arguments passed on it
 * @argc: the argument count
 * @argv: the argument array
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argv;

	printf("argc = %d\n", argc);

	return (0);
}
